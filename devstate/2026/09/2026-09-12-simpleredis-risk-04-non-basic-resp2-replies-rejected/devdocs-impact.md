# Devdocs impact
change: simpleredis-unsupported-reply

## Units
- SimpleRedis — subsystem — `simpleredis/` (`Eval` comment, `readReply` unsupported sentinel, exported `redis:unsupported-reply`)

## Findings
none.
