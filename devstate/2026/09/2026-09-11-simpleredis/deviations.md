# Deviations

- [x] taken  dest folder=package `simpleredis/` instead of README `redis/` / “Redis connection”
  Asked: Desired 4 — treat this as the next product library (README Planned Redis connection; layout `redis/`).
  Instead: folder and import last segment `simpleredis/`; Libraries table title SimpleRedis (exported type).
  Owner: `README.md`
  Why: dest sibling `reclaim/` kept the source package name and `folder == package`. Renaming `package simpleredis` to `redis` would add a variation to a copied API whose job is to stay that client.
  By: explore
  Requester: not asked
