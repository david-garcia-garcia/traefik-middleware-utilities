# Devdocs impact
change: harden-golangci-lint

## Units
- Test suites — subsystem — `knowledge/devdocs/std_go_test-suites.md` / CI lint job
- SimpleRedis — subsystem — identity `==` retry classifier (usage gotcha already on `std_go_simpleredis.md` and restated on test-suites)

## Findings
none.
