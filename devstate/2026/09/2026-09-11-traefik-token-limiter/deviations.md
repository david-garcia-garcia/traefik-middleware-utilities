# Deviations

- [x] taken  add a README token-bucket row instead of replacing a leaky-bucket row
  Asked: README: this is the leaky-bucket row’s replacement (token bucket, Traefik).
  Instead: dest already replaced leaky-`bucket/` with `windowcounter/` and reserved token bucket as a separate package; add a Token bucket row beside Window counter.
  Owner: `README.md`
  Why: honouring the letter would overwrite the window-counter row dest already shipped.
  By: explore
  Requester: not asked
