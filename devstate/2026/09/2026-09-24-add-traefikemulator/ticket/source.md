Add the traefikemulator package to github.com/david-garcia-garcia/traefik-middleware-utilities and open a pull request against master. The package is new upstream; it is not on origin/master. The implementation to bring across lives in the crowdsec bouncer repo at d:\repositories\crowdsec-bouncer-traefik-plugin\pkg\traefikemulator (emulator.go and zzz_emulator_test.go). Read those files only to ground what is being contributed. In THIS product repo, current behavior is not found until you measure the tree. Place the package the way this module already places packages (top-level, like reclaim and iplookup) unless the tree already uses another layout; do not invent a pkg/ folder. Extensive test coverage is required in the upstream PR: contributed tests plus any missing cases so the new package is covered the way sibling packages are. Do not modify the bouncer repo. Do not copy untracked scratch from the utilities main checkout. Switching the bouncer off its local copy is out of scope for this ticket.
